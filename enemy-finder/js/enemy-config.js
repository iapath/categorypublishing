/* Where the tool talks to. Both values are the publishable Supabase ones —
   they are meant to be in the browser. Never put a service_role key here.
   Same project as the Shelf Finder, so the same two values work. */
window.ENEMY_CONFIG = {
  supabaseUrl: "",            // https://xxxxxxxx.supabase.co
  supabaseAnonKey: "",        // anon / publishable key
  coachingUrl: "/coaching"
};
